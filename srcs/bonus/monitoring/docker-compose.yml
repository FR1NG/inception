version: '3.8'
services:
  node-exporter: 
    build: ./node-exporter/
    command:
      - '--path.procfs=/host/proc'
      - '--path.rootfs=/rootfs'
      - '--path.sysfs=/host/sys'
    volumes:
        - /proc:/host/proc:ro
        - /sys:/host/sys:ro
        - /:/rootfs:ro
    networks:
      - srcs_hchakoub_network
    ports:
      - "9100:9100"
  cadvisor:
    build: ./cadvisor/
    container_name: cadvisor
    ports:
      - "8080:8080"
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:rw
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
    networks:
      - srcs_hchakoub_network
  prometheus:
    build: ./prometheus/
    networks:
      - srcs_hchakoub_network
    ports:
      - "9090:9090"
  grafana:
    build: ./grafana/
    networks:
      - srcs_hchakoub_network
    ports:
      - "3000:3000"
networks:
  srcs_hchakoub_network:
    external: true
